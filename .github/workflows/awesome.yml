name: Awesome CI
on:
  - push
  - pull_request
  - schedule:
    - cron: 0 12 * * *
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1.0.0
      - uses: max/awesome-lint@1.0.0
        with:
          filename: README.md
